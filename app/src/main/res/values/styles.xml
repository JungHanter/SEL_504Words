<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryDark">@color/orange_dark</item>
        <item name="colorAccent">@color/white_orange</item>

        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:actionMenuTextColor">@android:color/black</item>
    </style>

    <!-- 툴바(액션바) 글씨 색 설정 ㅅㅂ -->
    <style name="AppTheme.Base.ToolbarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/orange</item>
        <item name="theme">@style/AppTheme.Base.ToolbarStyle.ActionBar</item>
        <item name="popupTheme">@style/AppTheme.Base.ToolbarStyle.Popup</item>
    </style>

    <!-- 툴바 타이틀 색 흰색할라고 ㅡㅡ -->
    <style name="AppTheme.Base.ToolbarStyle.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <!-- 툴바 옆에 메뉴 테마 다시 Light(흰배경에 검은글씨) 할라고 개 암 ㅁㅊ -->
    <style name="AppTheme.Base.ToolbarStyle.Popup" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/black</item>
    </style>

</resources>
